public class Search
{
    int start;
    int end;
    String name;
    int dem=0;
    public Search(){
        Scanner sc=new Scanner(System.in);
        System.out.println("nhap so id bat dau ");
        start=sc.nextint();
        System.out.println("nhap so id ket thuc ");
        start=sc.nextint();
        System.out.println("nhap so ten xe ");
        name=sc.nextln();
        for(int i=start;i<=end;i++;)
         if(car.id_car==i)
          (if(car.name==name) dem++;);;
          System.out.println(“so xe: “ + dem);
    }
}